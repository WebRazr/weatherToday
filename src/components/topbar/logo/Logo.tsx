import React, { FC } from "react";

const Logo: FC = () => {
  return (
      <div>
    <p style={{fontSize: '2em', fontFamily: "'Grandstander', cursive"}}>
     WeatherToday
    </p>
      </div>
  );
};

export default Logo;
