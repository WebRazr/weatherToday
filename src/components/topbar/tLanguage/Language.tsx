import React, { FC } from "react";

const Language: FC = () => {
    return (
        <div>
            <a href={'/ru'}>ru</a>
            <a href={'/en'}>en</a>
        </div>
    );
};

export default Language;
